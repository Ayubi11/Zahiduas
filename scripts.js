function bookHotel(hotelName) {
    alert(`Anda telah memesan ${hotelName}`);
}

// Placeholder functions for ticket booking (add more detailed implementation as needed)
function bookTicket(placeName) {
    alert(`Anda telah memesan tiket untuk ${placeName}`);
}
